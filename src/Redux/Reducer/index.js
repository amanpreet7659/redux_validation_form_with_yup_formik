// Root Reducer file 
// we have only one Root Reducer in whole project but reducer's can be multiple

import { combineReducers } from "redux";
import reducer from "./reducer";

export default combineReducers({
    reducer
});